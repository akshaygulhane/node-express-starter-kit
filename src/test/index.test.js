const assert = require('assert');

const indexRoute = require('./../routes/index.route');

describe('it should call the get api and return 200', () => {
    
    it('should get successfully', () => {

    })

})